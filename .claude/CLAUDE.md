# Claude Code bootstrap

- This repository uses `/AGENTS.md` as the canonical project guide. Read `/AGENTS.md` before drafting or revising any article.
- Prefer the repo-local skills `article-writer` and `article-refiner` from `.claude/skills/` for drafting and polishing posts.
- When diagrams help, follow the selection and embedding conventions in `/AGENTS.md`; this repo supports image syntax, fenced code blocks, shortcode `src`, and inline shortcode forms for Mermaid, Chart.js, Excalidraw, and Draw.io.
- Default output language is Simplified Chinese unless the user explicitly asks for English or bilingual output.
